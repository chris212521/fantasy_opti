  <div class="container-fluid search-criteria">	

	<form action="/optimal_lineup/<%= params[:site] %>" method="get" autocomplete="off">
	  	  <div class="row"><h4><%= label_tag(:company_name, "Player Filters:") %></h4></div>
	  	  <div class="row"><%= text_field_tag :salary, params[:salary], placeholder: "Salary Available", class: "search-form-text-field" %></div>
	  	  <div class="row"><%= label_tag(:company_name, "Positions Needed:") %>

	<%= select_tag(:num_positions, options_for_select(NFL.supported_searchable_pos, params[:num_positions]))%>
</div>
	  	  
			<%= select_tag "position1", options_for_select(NFL.dk_positions,params[:position1]) %>
			<%= select_tag "position2", options_for_select(NFL.dk_positions, if params[:position2].nil? then 'RB' else params[:position2] end) %>
			<%= select_tag "position3", options_for_select(NFL.dk_positions, if params[:position3].nil? then 'WR' else params[:position3] end) %>
			<%= select_tag "position4", options_for_select(NFL.dk_positions, if params[:position4].nil? then 'TE' else params[:position4] end) %>

	<div class="col-md-12"> 
	  <%= submit_tag("Search", :name => '', :class=>"btn btn-search") %>
	  </div>
   </div>
   </form>

<script>
$(function(){
    $('#num_positions').change(function() {
        var selectValue = $(this).val();
        if (selectValue > 3){
         	$('#position3,#position4').show();
         	$('#position3,#position4').removeClass('toBeRemoved');
        }else if (selectValue > 2){
        	$('#position3').show();
        	$('#position3').removeClass('toBeRemoved');
         	$('#position4').hide();
         	$('#position4').addClass('toBeRemoved');
        }else if (selectValue > 1){
         	$('#position4,#position3').hide();
         	$('#position4,#position3').addClass('toBeRemoved');
        }
    });
});

$('form').submit(function(){
    $('.toBeRemoved').remove();
});

$(document).ready(function () {
  $('#num_positions').change();
});
</script>